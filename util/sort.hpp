/**
 * @brief
 * @note
 * */

template<typename type> void quick_sort(const type* ptr, unsigned long long from, unsigned long long end){
    //while(from < end){
    //    if(ptr[from] < )
    //}
};


template<typename type> void a(type c){};